module.exports = {
    cleanText:function(text) {
        // clean it and return
    },

    isBodyPresent(response) {
     
        if (!response) {
            console.log("not")
            return "Content cannot be empty";
      
        }
        else{
            return 0;
        }
    }
}